<h1>Listing dashboard</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Date</th>
  </tr>
 <% if !@dashboards.nil?
    @dashboards.each do |dashboard| %>
    <tr>
      <td><%= dashboard.title %></td>
      <td><%= dashboard.dateArchive %></td>
      <td><%= link_to 'Show', dashboard_path(dashboard) %></td>
    </tr>
  <% end
 end %>
  
</table>

<%= link_to 'New dashboard', new_dashboard_path %>